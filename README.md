# MathRAG

**Retrieval-Augmented Generation система для вопросно-ответной работы с учебниками по математике**

RAG-система, которая позволяет задавать вопросы по материалам учебников. Система ищет ответы в загруженных PDF-документах и генерирует ответы с помощью локальной языковой модели через LM Studio.

## Ключевые особенности

- **Поддержка PDF-документов**: Автоматическая конвертация PDF в текст с сохранением структуры
- **Семантический поиск**: Поиск по векторной базе данных ChromaDB с использованием эмбеддингов
- **Локальная языковая модель**: Работа через LM Studio с поддержкой streaming-ответов
- **Два интерфейса**: 
  - Web-интерфейс на Gradio
  - Telegram-бот

## 📁 Структура проекта

```
MathRAG/
├── src/
│   ├── __init__.py
│   ├── config.py          # Конфигурация путей и параметров
│   ├── ingest.py          # Загрузка и обработка документов
│   └── rag_engine.py      # Основная логика RAG-системы
├── data/                  # Папка для PDF-документов (создается автоматически)
├── chroma_db/             # Векторная база данных (создается автоматически)
├── app.py                 # Gradio web-интерфейс
├── bot.py                 # Telegram-бот
├── requirements.txt       # Зависимости Python
└── README.md             # Этот файл
```

## ⚙️ Установка и настройка

### 1. Клонирование репозитория
```bash
git clone <your-repository-url>
cd MathRAG
```

### 2. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 3. Настройка LM Studio
1. Установите [LM Studio](https://lmstudio.ai/)
2. Загрузите модель (например, Qwen или Llama)
3. Запустите сервер:
   - Нажмите "Start Server"
   - Убедитесь, что адрес: `http://127.0.0.1:1234/v1`

### 4. Подготовка документов
1. Поместите PDF-файлы с учебниками в папку `data/`
   ```bash
   mkdir -p data
   cp ваши_учебники.pdf data/
   ```
2. Запустите обработку документов:
   ```bash
   python -m src.ingest
   ```

## Запуск

### Web-интерфейс (Gradio)
```bash
python app.py
```
Приложение будет доступно по адресу: `http://localhost:7860`

### Telegram-бот
1. Получите токен бота у [@BotFather](https://t.me/botfather)
2. Создайте файл .env и копируйте в него содержимое файла .env_example. Укажите свой токен телеграм бота.
3. Запустите бота:
   ```bash
   python bot.py
   ```

## 🔧 Конфигурация

Основные настройки в `src/config.py`:

| Параметр | Описание | Значение по умолчанию |
|----------|----------|----------------------|
| `LM_STUDIO_URL` | Адрес локального сервера LM Studio | `"http://127.0.0.1:1234/v1"` |
| `EMBEDDING_MODEL` | Модель для эмбеддингов | `"sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2"` |
| `CHUNK_SIZE` | Размер чанков при разбиении текста | `1000` |
| `CHUNK_OVERLAP` | Перекрытие между чанками | `200` |

## 📖 Как это работает

1. **Ингestion** (`ingest.py`):
   - PDF-документы конвертируются в Markdown
   - Текст разбивается на перекрывающиеся чанки
   - Генерируются векторные эмбеддинги и сохраняются в ChromaDB

2. **Поиск** (`rag_engine.py`):
   - Вопрос пользователя преобразуется в эмбеддинг
   - Находится 10 наиболее релевантных чанков
   - Контекст передается в языковую модель

3. **Генерация ответа**:
   - LLM получает контекст и вопрос
   - Формируется ответ без использования LaTeX
   - Удаляются служебные теги `<think>...</think>`

## Технологический стек

- **LangChain** — работа с LLM и цепочками
- **ChromaDB** — векторная база данных
- **Hugging Face Transformers** — модели эмбеддингов
- **Gradio** — web-интерфейс
- **Aiogram** — Telegram-бот
- **MarkItDown** — конвертация PDF в Markdown
